import React, { useState } from 'react';
import CinemaHall from '../components/CinemaHall';
const Booking = ({ }) => {
    return (
        <div className="">
            <CinemaHall />
        </div>
    );
};

export default Booking;